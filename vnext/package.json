{
  "name": "devspace-ultra",
  "version": "2.0.1",
  "description": "Secure ChatGPT Remote Tool Gateway + Outbound Local Agent + Kaggle Backend + Durable Task System",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "gateway": "ts-node src/cli/gateway-cli.ts",
    "agent": "ts-node src/cli/agent-cli.ts",
    "test": "ts-node tests/run-all-tests.ts",
    "test:unit": "ts-node tests/run-unit-tests.ts",
    "test:integration": "ts-node tests/run-integration-tests.ts",
    "test:security": "ts-node tests/run-security-tests.ts"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^20.12.12",
    "@types/ws": "^8.5.10",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
