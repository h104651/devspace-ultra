# MCP Insufficient-Scope Step-Up Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make authenticated MCP tool calls that lack a required OAuth scope return the standard HTTP 403 Bearer `insufficient_scope` challenge so ChatGPT can perform OAuth scope step-up and retry.

**Architecture:** Keep the existing OAuth allowlist and task-level authorization boundaries unchanged. Convert only `AUTH_FORBIDDEN` tool-call failures with a determinable missing scope into an HTTP-layer OAuth challenge, preserving normal MCP tool errors for non-authorization failures. The challenge advertises the missing scope and the existing RFC 9728 protected-resource metadata URL.

**Tech Stack:** TypeScript, Cloudflare Durable Objects, MCP 2026-07-28 stateless HTTP transport, OAuth 2.1 / RFC 6750 / RFC 9728, Node assert test harness, GitHub Actions.

**Spec:** MCP scope step-up behavior (SEP-2350 / current MCP SDK behavior) and RFC 6750 `insufficient_scope` semantics.

## Global Constraints

- Do not auto-grant `local:write` or weaken `TaskRouter` scope checks.
- Do not add `local:exec`, `raw_shell:run`, or admin scopes.
- Preserve existing successful tool-call response behavior.
- Preserve ordinary non-authentication tool errors as MCP `isError` tool results.
- For authenticated insufficient-scope failures, emit HTTP 403 plus `WWW-Authenticate: Bearer error="insufficient_scope"` with the missing `scope` and same-origin `resource_metadata` URL.
- Follow TDD: regression test must fail on the current base before production code changes.
- Base branch is `feature/vnext-remote-gateway` at `75f52b2a43c6283119bcbeea2579bb12d9cb6cbb`.

---

### Task 1: Reproduce the missing step-up challenge

**Files:**
- Create: `vnext/tests/cloudflare/mcp-insufficient-scope.test.ts`
- Modify: `vnext/tests/run-all-tests.ts`

**Interfaces:**
- Consumes: `GatewayDurableObject.fetch()` and MCP 2026-07-28 request headers/body.
- Produces: regression assertion for HTTP 403 `WWW-Authenticate` scope challenge.

- [ ] **Step 1: Write failing test** using a valid access token with `mcp:access`, `tasks:submit`, and `local:read` but without `local:write`; call `local_write_file` through `/mcp`.
- [ ] **Step 2: Assert desired response** is HTTP 403, challenge contains `error="insufficient_scope"`, `scope="local:write"`, and `resource_metadata="<gateway>/.well-known/oauth-protected-resource/mcp"`.
- [ ] **Step 3: Run full CI and verify RED** because the current code returns HTTP 200 with an MCP `isError` tool result.

### Task 2: Emit standards-compliant scope challenge

**Files:**
- Modify: `vnext/src/cloudflare/gateway-durable-object.ts`

**Interfaces:**
- Produces: an HTTP `Response` for determinable `AUTH_FORBIDDEN` insufficient-scope failures.

- [ ] **Step 1: Add a narrow helper** that extracts explicitly missing quoted scopes from the existing `AUTH_FORBIDDEN ... Missing: 'scope'` error generated by `TaskRouter`.
- [ ] **Step 2: In `tools/call` catch**, return HTTP 403 with an RFC 6750 Bearer challenge only when a missing scope was deterministically extracted.
- [ ] **Step 3: Keep all other exceptions on the existing MCP `isError` response path.**
- [ ] **Step 4: Run full CI and verify GREEN.**

### Task 3: Integration readiness

**Files:**
- No additional production changes unless verification exposes a regression.

- [ ] **Step 1: Confirm build, full tests, and Wrangler deploy dry-run pass.**
- [ ] **Step 2: Review the PR diff for any scope broadening or unrelated behavior changes.**
- [ ] **Step 3: Merge only after green CI; production deployment remains a separate rollout step.**
